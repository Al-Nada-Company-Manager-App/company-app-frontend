

const ModalStyle = () => {
  return (
    <style>{`
      /* Legacy Ant Design Modal overrides have been removed. */
      /* Modals are now built natively with AppModal using Tailwind CSS. */
    `}</style>
  );
};

export default ModalStyle;
