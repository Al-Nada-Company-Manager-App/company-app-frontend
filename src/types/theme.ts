export interface ThemeConfig {
  backgroundImage: string;
  containerBg: string;
  textColor: string;
  buttonStyles: {
    backgroundColor: string;
    color: string;
  };
  gradient1: string;
  gradient2: string;
}
